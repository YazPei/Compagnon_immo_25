# docker-compose.yml — Compagnon Immo (version corrigée)
# - Aligne chemins Dockerfile/scripts avec le Makefile
# - Fixe MLflow (backend + artifact root) et healthcheck
# - Uniformise mounts, users, networks, depends_on (service_healthy)
# - Ajoute des profiles (regression, series) pour cibler les runs

services:
<<<<<<< HEAD:docker-compose.yml
  # --------- Tracking MLflow ----------
  mlflow:
    image: ghcr.io/mlflow/mlflow:latest
    container_name: compagnon_immo-mlflow
    ports:
      - "5000:5000"
    volumes:
      - ./mlruns:/mlflow/mlruns
    command: [
      "mlflow","server",
      "--backend-store-uri","sqlite:///mlflow.db",
      "--default-artifact-root","/mlflow/mlruns",
      "--host","0.0.0.0",
      "--port","5000"
    ]
    environment:
      GUNICORN_CMD_ARGS: "--workers 1 --timeout 180 --graceful-timeout 180 --log-level info"
    networks:
      - ml_net
    healthcheck:
      test: ["CMD-SHELL", "python -c \"import urllib.request,sys; urllib.request.urlopen('http://localhost:5000'); sys.exit(0)\" || exit 1"]
      interval: 20s
      timeout: 5s
      retries: 15
    restart: unless-stopped


=======
>>>>>>> a394f52ab3dfc789a642fdbd9964a4d3fcba8550:infra/deployment/docker-compose.yml
  # --------- Pipeline de lancement ----------
  run_full:
    build:
      context: .
      dockerfile: mlops/1_import_donnees/Dockerfile.run
    user: "${UID:-1000}:${GID:-1000}"
    volumes:
      - .:/app
    tty: true
    stdin_open: true
    environment:
      - RUN_MODE=full
      - MLFLOW_TRACKING_URI=http://mlflow:5000
    depends_on:
      mlflow:
        condition: service_healthy
    networks:
      - ml_net

  # --------- Préparation des données ----------
  fusion_geo:
    build:
      context: .
      dockerfile: mlops/3_fusion/Dockerfile.fusion
    user: "${UID:-1000}:${GID:-1000}"
    volumes:
      - .:/app
      - ./app/mlops:/app/mlops
    environment:
      - MLFLOW_TRACKING_URI=http://mlflow:5000
    depends_on:
      mlflow:
        condition: service_healthy
    networks:
      - ml_net
    command: ["bash", "-lc", "python mlops/3_fusion/fusion.py"]
    profiles: ["regression", "series"]

  preprocessing:
    build:
      context: .
      dockerfile: mlops/preprocessing_4/Dockerfile.preprocessing
    image: compagnon_immo-preprocessing
    user: "${UID:-1000}:${GID:-1000}"
    volumes:
      - .:/app
      - ./data:/app/data


    command: >
      python /app/mlops/preprocessing_4/preprocessing.py
      --input-path /data
      --output-folder1 /app/data
      --output-folder2 /app/data
    environment:
      - MLFLOW_TRACKING_URI=http://mlflow:5000
    depends_on:
      mlflow:
        condition: service_healthy
    networks:
      - ml_net
    profiles: ["regression", "series"]

  clustering:
    build:
      context: .
      dockerfile: mlops/5_clustering/Dockerfile.clustering
    user: "${UID:-1000}:${GID:-1000}"
    volumes:
      - ./data:/app/data
      - ./app/mlops:/app/mlops
    environment:
      - MLFLOW_TRACKING_URI=http://mlflow:5000
    depends_on:
      mlflow:
        condition: service_healthy
    networks:
      - ml_net

    command: >
      python /app/mlops/clustering/Clustering.py
      --input-path data/train_clean.csv
      --output-path1 data/df_cluster.csv
      --output-path2 data/df_sales_clean_ST.csv


  encode:
    build:
      context: .
      dockerfile: mlops/6_Regression/1_Encoding/Dockerfile.encoding.REG
    user: "${UID:-1000}:${GID:-1000}"
    volumes:
      - ./app/mlops/Regression:/app/mlops/Regression
      - ./data:/app/data
      - ./exports:/app/exports
    command: ["python", "/app/mlops/6_Regression/1_Encoding/encoding.py"]
    environment:
      - MLFLOW_TRACKING_URI=http://mlflow:5000
    depends_on:
      mlflow:
        condition: service_healthy
    networks:
      - ml_net

  # --------- Régression ----------
  train_lgbm:
    build:
      context: .
      dockerfile: mlops/6_Regression/2_LGBM/Dockerfile.lgbm.REG
    user: "${UID:-1000}:${GID:-1000}"
    volumes:
      - ./app/mlops/Regression:/app/mlops/Regression
      - ./exports:/app/exports
    command: ["python", "/app/mlops/6_Regression/2_LGBM/train_lgbm.py"]
    environment:
      - MLFLOW_TRACKING_URI=http://mlflow:5000
    depends_on:
      mlflow:
        condition: service_healthy
    networks:
      - ml_net

  analyse:
    build:
      context: .
      dockerfile: mlops/6_Regression/4_Analyse/Dockerfile.analyse.REG
    user: "${UID:-1000}:${GID:-1000}"
    volumes:
      - ./app/mlops:/app/mlops
      - ./exports:/app/exports
      - ./data:/app/data
    command: ["python", "/app/mlops/6_Regression/4_Analyse/analyse.py"]
    environment:
      - MLFLOW_TRACKING_URI=http://mlflow:5000
    depends_on:
      mlflow:
        condition: service_healthy
    networks:
      - ml_net
    profiles: ["regression"]

  # --------- Séries Temporelles ----------
  split:
    build:
      context: .
      dockerfile: mlops/7_Serie_temporelle/1_SPLIT/Dockerfile.split.ST
    user: "${UID:-1000}:${GID:-1000}"
    volumes:
      - ./exports:/app/exports
      - ./app/mlops:/app/mlops
    entrypoint: [ "python", "/app/mlops/Serie_temporelle/load_split.py" ]

    environment:
      - MLFLOW_TRACKING_URI=http://mlflow:5000
      - RUN_MODE=full
      - ST_SUFFIX=${ST_SUFFIX:-q12}
    depends_on:
      mlflow:
        condition: service_healthy
    networks:
      - ml_net
    profiles: ["series"]

  decompose:
    build:
      context: .
      dockerfile: mlops/7_Serie_temporelle/2_Decompose/Dockerfile.decompose.ST
    user: "${UID:-1000}:${GID:-1000}"
    volumes:
      - ./exports:/app/exports
      - ./app/mlops:/app/mlops
    command: >
      python /app/mlops/Serie_temporelle/seasonal_decomp.py
      --input-folder /app/exports/st
      --output-folder /app/exports/st
    environment:
      - MLFLOW_TRACKING_URI=http://mlflow:5000
      - RUN_MODE=full
      - ST_SUFFIX=${ST_SUFFIX:-q12}
    depends_on:
      mlflow:
        condition: service_healthy
    networks:
      - ml_net
    profiles: ["series"]

  train_sarimax:
    build:
      context: .
      dockerfile: mlops/7_Serie_temporelle/3_SARIMAX/Dockerfile.sarimax.ST
    user: "${UID:-1000}:${GID:-1000}"
    volumes:
      - ./exports:/app/exports
      - ./app/mlops:/app/mlops
    command: >
      python /app/mlops/Serie_temporelle/sarimax_train.py
      --input-folder /app/exports/st
      --output-folder /app/exports/st
    environment:
      - MLFLOW_TRACKING_URI=http://mlflow:5000
      - RUN_MODE=full
      - ST_SUFFIX=${ST_SUFFIX:-q12}
    depends_on:
      mlflow:
        condition: service_healthy
    networks:
      - ml_net

  evaluate:
    build:
      context: .
      dockerfile: mlops/7_Serie_temporelle/4_EVALUATE/Dockerfile.evaluate.ST
    user: "${UID:-1000}:${GID:-1000}"
    volumes:
      - ./exports:/app/exports
      - ./app/mlops:/app/mlops
      - ./data:/app/data
    entrypoint: ["python", "/app/mlops/7_Serie_temporelle/4_EVALUATE/evaluate_ST.py"]
    environment:
      - MLFLOW_TRACKING_URI=http://mlflow:5000
      - RUN_MODE=full
      - ST_SUFFIX=${ST_SUFFIX:-q12}
    depends_on:
      mlflow:
        condition: service_healthy
    networks:
      - ml_net

  series_pipeline:
    build:
      context: .
      dockerfile: mlops/7_Serie_temporelle/Dockerfile.series_pipeline
    user: "${UID:-1000}:${GID:-1000}"
    volumes:
      - ./app/mlops:/app/mlops
      - ./data:/app/data
      - ./exports:/app/exports
    command: ["python", "/app/mlops/7_Serie_temporelle/pipeline.py", "--help"]
    networks:
      - ml_net
    profiles: ["series"]


  # --------- Tracking MLflow ----------
  mlflow:
    image: ghcr.io/mlflow/mlflow:latest
    container_name: compagnon_immo-mlflow
    ports:
      - "5000:5000"
    volumes:
      - ./mlruns:/mlflow/mlruns
    command: [
      "mlflow","server",
      "--backend-store-uri","sqlite:///mlflow.db",
      "--default-artifact-root","/mlflow/mlruns",
      "--host","0.0.0.0",
      "--port","5000"
    ]
    environment:
      GUNICORN_CMD_ARGS: "--workers 1 --timeout 180 --graceful-timeout 180 --log-level info"
    networks:
      - ml_net
    healthcheck:
      test: ["CMD-SHELL", "python -c \"import urllib.request,sys; urllib.request.urlopen('http://localhost:5000'); sys.exit(0)\" || exit 1"]
      interval: 20s
      timeout: 5s
      retries: 15
    restart: unless-stopped      


  # --------- DVC ----------
  dvc-runner:
    build:
      context: .
      dockerfile: app/mlops/2_dvc/Dockerfile.dvc

    volumes:
      - .:/app
      - ~/.dvc:/app/.dvc
    working_dir: /app
    tty: true
    stdin_open: true
    command: ["sh", "-lc", "./run_dvc.sh"]
    depends_on:
      mlflow:
        condition: service_started  
    environment:
      - MLFLOW_TRACKING_URI=http://mlflow:5000
    networks:
      - ml_net


networks:
  ml_net:
    driver: bridge

